// Export the Request namespace functionality
export type { RequestSpec } from './requests';

// The request handler is automatically registered with the namespace system
// No need to export handler functions as they're used internally